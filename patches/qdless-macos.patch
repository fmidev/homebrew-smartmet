diff --git a/include/QdlessApp.h b/include/QdlessApp.h
index cbf8f7d..e17bdcb 100644
--- a/include/QdlessApp.h
+++ b/include/QdlessApp.h
@@ -14,12 +14,21 @@
 
 namespace Qdless
 {
+// Default data dir baked at build time. -DQDLESS_DATA_DIR=... overrides;
+// otherwise falls back to the upstream /usr/share/smartmet/qdless layout.
+#ifndef QDLESS_DATA_DIR
+#define QDLESS_DATA_DIR "/usr/share/smartmet/qdless"
+#endif
+#ifndef QDLESS_GSHHG_DIR
+#define QDLESS_GSHHG_DIR "/usr/share/gshhg-gmt-nc4"
+#endif
+
 struct Options
 {
   std::string filename;
-  std::string paletteDir = "/usr/share/smartmet/qdless/palettes";
-  std::string configFile = "/usr/share/smartmet/qdless/qdless.conf";
-  std::string coastlineDir = "/usr/share/gshhg-gmt-nc4";
+  std::string paletteDir = QDLESS_DATA_DIR "/palettes";
+  std::string configFile = QDLESS_DATA_DIR "/qdless.conf";
+  std::string coastlineDir = QDLESS_GSHHG_DIR;
   std::string parameterOverride;  // empty -> use first parameter
   std::string paletteOverride;    // empty -> resolve via config / built-in
   int timeIndex = 0;              // 0-based; -1 means last
diff --git a/source/QdlessApp.cpp b/source/QdlessApp.cpp
index 1e1e2a7..3106efd 100644
--- a/source/QdlessApp.cpp
+++ b/source/QdlessApp.cpp
@@ -787,7 +787,7 @@ bool App::ensureCityIndex() const
   itsCityIndexAttempted = true;
 
   std::vector<std::filesystem::path> candidates{
-      std::filesystem::path("/usr/share/smartmet/qdless/cities1000.tsv"),
+      std::filesystem::path(QDLESS_DATA_DIR) / "cities1000.tsv",
   };
   try
   {
diff --git a/source/QdlessDataSource.cpp b/source/QdlessDataSource.cpp
index 7d1685e..a21c33f 100644
--- a/source/QdlessDataSource.cpp
+++ b/source/QdlessDataSource.cpp
@@ -1,6 +1,8 @@
 #include "QdlessDataSource.h"
 
+#ifndef QDLESS_NO_GRID_FILES
 #include "QdlessGridFilesSource.h"
+#endif
 #include "QdlessQueryDataSource.h"
 
 #include <fstream>
@@ -43,7 +45,13 @@ std::unique_ptr<DataSource> DataSource::open(const std::string& filename)
       return std::make_unique<QueryDataSource>(filename);
     case FileKind::kGrib:
     case FileKind::kNetCDF:
+#ifdef QDLESS_NO_GRID_FILES
+      throw std::runtime_error(
+          "GRIB / NetCDF input requires a build with smartmet-library-grid-files; "
+          "this binary supports QueryData (.sqd) only");
+#else
       return std::make_unique<GridFilesSource>(filename);
+#endif
     case FileKind::kUnknown:
       break;
   }
